# Per-engagement scan outputs + analyst state — kept outside version control:
# big, target-specific, and contain raw evidence (logs, request bodies, real
# hostnames/IPs). Never commit these to a public repo.
pentest_*/
*_validation_review.md
asset_tags.json
suppressions.json
domains_*.txt
scope*.txt
known_hosts.txt
known_ips.txt
subfinder-provider-config.yaml
# Auth: user store + signing secret — credentials, never commit
.berckley_users.json
.berckley_secret

# Local editor / OS noise
.vscode/
.idea/
*.swp
*~
.DS_Store

# Temporary regenerator helpers
/tmp/regen_report_md.sh

# Python virtualenvs & bytecode caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
